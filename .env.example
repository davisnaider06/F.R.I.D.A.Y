# Copie este arquivo para .env e preencha os valores.
# O .env fica fora do git (.gitignore); este aqui entra.

# Chave da API da Anthropic — console.anthropic.com > API keys.
# O nome é lido pelo SDK automaticamente; não invente outro.
ANTHROPIC_API_KEY=
